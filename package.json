{
  "name": "fashion-store",
  "version": "1.0.0",
  "description": "Fashion Store E-Commerce Application",
  "engines": {
    "node": "18.19.0"
  },
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps --force && ng build --configuration=production"
  },
  "private": true
}
